import { css } from 'lit';

export const styles = css`
  .description {
    padding-bottom: 2em;
  }
  .value {
    display: flex;
    align-content: center;
    flex-wrap: wrap;
  }

  ha-icon-button {
    color: var(--primary-color);
  }

  ha-settings-row {
    padding: 0;
  }
`;
