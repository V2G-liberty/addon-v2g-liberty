style:
  ha-textfield $: |
    .mdc-text-field {
      height: 40px !important;
    }
    .mdc-text-field__affix {
      height: 40px !important;
      line-height: 40px;
      width: 45px !important;
      text-align: left;
    }
    .mdc-text-field__input {
      align-self: end;
      text-align: right;
      width: 70px !important;
    }
    input::-webkit-outer-spin-button,
    input::-webkit-inner-spin-button {
      -webkit-appearance: none;
      margin: 0;
    }
    input[type=number] {
      -moz-appearance: textfield;
    }
    .mdc-text-field__affix:hover {
      color: red;
    }
  # TODO: import from icon_icon_style.yaml?
  hui-generic-entity-row $: |
    div::before {
      content: "ⓘ";
      font-size: 150%;
      font-weight: bold;
      position: absolute;
      top: 10px;
      right: 14px;
      z-index: 100;
      color:  var(--primary-color);
    }

